---
import Button from '@/components/atoms/Button.vue';
import { getLocale, t , useTranslatedPath, getLangFromUrl } from '../../utils/i18n';
import { Image } from 'astro:assets';
import BlockIcon from '../icons/BlockIcon.vue';

const lang = getLangFromUrl(Astro.url);

---

<div class="intro-home">
  <div class="intro-home__text">
    <p class="intro-home__text__p">We are so excited to celebrate our special day with you in beautiful Murcia, Spain!</p>
    <p class="intro-home__text__p">We are so excited to celebrate our special day with you in beautiful Murcia, Spain! </p>
    <p class="intro-home__text__p">We cannot wait to celebrate our love surrounded by cherished family and friends from all around the world. </p>
  </div>
  <Button extraClass='intro-home__button' href=`/rsvp` label='RSVP'/>
  <div class="intro-home__figures-container">
    <Image class={"intro-home__figures-container__marco"} src={'/src/assets/marco1.png'} alt="Marco" width={300} height={400} />

    <figure class="intro-home__figure">
    <Image src={'/src/assets/alex-maria02.jpg'} alt="Maria and Alex" width={200} height={300} />
  </figure>
  </div>
  <!-- <BlockIcon -->
</div>
<style lang="scss">
.intro-home {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 2rem;
  margin-top: 3rem;

  &__text {
    font-family: $font-cormorant;
    font-weight: bold;
    font-size: $font-size-md;
    color: $color-red;
    margin-bottom: 5rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;

  }

  &__button {
    margin-bottom: 5rem;
    width: 9rem;
  }

  &__figures-container {
    position: relative;
    display: flex;

    &__marco {
      position: absolute;
      width: 100%;
      height: auto;
      top: 1rem;
      left: 0rem;
      z-index: -1;
    }
  }

  &__figure {
    width: 100%;
    max-width: 32rem;
    padding-left: 6%;
    padding-right: 9%;

    & img {
      display: block;
      width: 100%;
      max-width: 100%;
      height: auto;
      border: 1rem solid $color-white; border-width: 1rem 1rem 2.5rem 1rem;
    }
  }
}

</style>