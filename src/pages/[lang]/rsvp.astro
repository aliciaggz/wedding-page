---
import RsvpForm from '@/components/organism/rsvp/RsvpForm.vue';
import Empty from "@/layouts/Empty.astro";

import '../../styles/global.scss';

import { getLocale, t , useTranslatedPath, getLangFromUrl } from '../../utils/i18n';

const lang = getLangFromUrl(Astro.url);
// const lang = getLangFromUrl(Astro.url);

const locale = getLocale(lang);
const translatePath = useTranslatedPath(lang);


//lo tengo que poner si o si para que funcione la generacion de rutas estaticas
export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
    { params: { lang: "es" } },
  ];
}

---

<Empty>
	<RsvpForm client:load />
</Empty>

